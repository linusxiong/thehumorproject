# Git 提交规范

- 提交信息使用 `<type>(<scope>): <简短描述>` 格式，scope 必须填写，冒号后保留一个空格。
- 根据实际改动选择类型：`feat`（新功能）、`fix`（修复）、`docs`（文档）、`refactor`（重构）、`test`（测试）、`chore`（维护）。
- scope 使用受影响的模块，例如 `project`、`app`、`config`；描述应简洁说明本次改动。
- 示例：`feat(project): initialize Next.js app at repository root`、`fix(app): correct home page layout`、`docs(agent): document commit conventions`。
